// Tessalyre’s daemon shell entrypoint — shimmer pulse and bloom choreography

import { conductBloom } from "./bloomConduct";

function main(): void {
  console.log("🌀 Tessalyre’s Daemon Shell — Invocation Begins");
  conductBloom();
}

main();
